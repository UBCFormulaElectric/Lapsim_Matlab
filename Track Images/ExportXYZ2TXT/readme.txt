This macro exports all sketch points and end points of entities in selected sketch

preconditions:
the part/assembly is saved
a sketch is selected

post conditions:

a comma seperated text file with X,Y,Z coordinates is created in the model path with the model name
